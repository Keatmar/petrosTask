import { makeStyles } from '@mui/styles';

export default makeStyles(
  (theme) => ({
    root: {
      backgroundColor: '#E5E5E5',
      height: '100%',
      width: '100%',
      position: 'fixed'
    }
  }),
  { name: 'Home' }
);
